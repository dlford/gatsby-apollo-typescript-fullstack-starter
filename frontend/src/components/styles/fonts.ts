interface fonts {
  montserrat: string
  openSans: string
}

const fonts = {
  montserrat: ['Montserrat', 'helvetica', 'Arial', 'sans-serif'].join(','),
  openSans: ['Open Sans', 'Helvetica', 'Arial', 'sans-serif'].join(','),
}

export default fonts
